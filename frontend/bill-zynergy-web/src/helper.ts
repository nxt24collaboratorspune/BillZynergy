import { upload } from "@testing-library/user-event/dist/upload";

const paths = {
    welcome: '/',
    upload: '/upload',
    dashboard: '/dashboard',
    reports: '/reports',
    subscription: '/subscription'
}

export { paths };